# Mission 03 — Hybrid ML with Provable Accuracy

Read `AGENTS.md`. Use the bundled `$machine-learning` skill; delegate to the `ml-engineer` subagent for training/eval. Non-interactive: print `MISSION 03 FAILED: <reason>` on unrecoverable errors. Check `TEST.ENV_PROBES` first — if `anomaly_detection_available` = FAIL, use the SQL z-score fallback noted below and record that decision.

## Objective

Early-warning anomaly layer + failure classifier + risk fusion, evaluated honestly against `TEST.GROUND_TRUTH_FAILURES`.

## Deliverables

1. `sql/05_ml_models.sql` — executed:
   - 3 multi-series `SNOWFLAKE.ML.ANOMALY_DETECTION` models — one per signal family (vibration_rms, temp_c, rpm), each with `series_column = 'ASSET_ID'`, trained on healthy windows only (exclude ground-truth degradation spans from training data by date filter; do NOT join labels into scoring features — training SQL may reference TEST.GROUND_TRUTH_FAILURES solely for the date-range exclusion WHERE clause).
   - Scoring procedure writing to `ML.ANOMALY_EVENTS` (distance, percentile, is_anomaly, bounds) + `TASK_DETECT_ANOMALIES` every 5 minutes on `AEGIS_WH` (create SUSPENDED; resume only at the end if all tests pass).
   - `SNOWFLAKE.ML.FORECAST` on hourly vibration + OEE for the digital-twin trend bands → `ML.SIGNAL_FORECASTS`.
   - Fallback if Cortex ML unavailable: rolling z-score + persistence detector in pure SQL with the same output contract.
2. ~~Stretch (SKIPPED per planning decision D8): `ml/train_classifier.py` XGBoost classifier. Not built in the main pass — anomaly detection + z-score fusion is sufficient. May revisit if ahead of schedule.~~
3. Risk fusion: update `FEATURES.DT_ASSET_HEALTH` (or a small serving view) to combine anomaly distance, classifier probability (if present), trend persistence, criticality → health_score, failure_probability_24h, predicted_mode, risk_level.
4. `tests/ml_recall_check.sql` — event-level evaluation vs ground truth with episode-based splits: recall, precision, false alerts per asset-day, median lead time (hours before failure_ts), mode F1; comparison row for the fixed-threshold baseline. Persist to `TEST.ML_METRICS` and print the table.
5. Append run record + metrics table to `docs/run-records.md`.

## Acceptance criteria

- Zero label leakage (assert no feature reads TEST.GROUND_TRUTH_FAILURES).
- Recall ≥ 0.8 on injected failure episodes with median lead ≥ 24h; if unmet after tuning, report best achieved honestly with tuning notes — do not fabricate.
- Golden-path episode (CNC_01_SPINDLE BEARING_WEAR) detected with ≥ 48h lead.

Print `MISSION 03 COMPLETE` when done.
